"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.exists = void 0;
var dictionary_1 = require("./dictionary");
var exists = function (text) {
    if (typeof text !== "string") {
        throw new TypeError("Text must be of type string.");
    }
    var cleanedText = text.trim().toLowerCase();
    if (cleanedText.length === 0) {
        return false;
    }
    if (cleanedText.length === 1) {
        return cleanedText === "i" || cleanedText === "a";
    }
    var wordPrefixGroup = dictionary_1.dictionary[cleanedText.slice(0, 2)];
    return Boolean(wordPrefixGroup === null || wordPrefixGroup === void 0 ? void 0 : wordPrefixGroup.includes(cleanedText));
};
exports.exists = exists;
